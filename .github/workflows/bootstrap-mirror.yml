name: bootstrap-mirror (retired)
# 一次性镜像已于 run 34660876099 完成（master = 上游 e8f6f263 压缩导入）。
# 本文件退役为惰性占位：无 workflow_dispatch、push 触发指向永不存在的分支。
# （工具无删文件能力，故以惰性化代替删除；可随时手动删除本文件。）
on:
  push:
    branches: ["__never__"]

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "retired one-time bootstrap; mirror already completed"
