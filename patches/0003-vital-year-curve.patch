# 本文件已拆分（09-12）

单文件多 hunk 手工数行连续出错（v1 漏逗号、v2 行数头错），失败半径=整轮 CI。
现按 4 个单 hunk 小补丁应用（见 .github/workflows/umasim-test.yml bench 步骤）：

- `0003-a-struct-field.patch` — struct 字段 + 尾逗号
- `0003-b-default-impl.patch` — Default impl
- `0003-c-dynamic-vital.patch` — 分年价格曲线 + 回体力对称计值
- `0003-d-token.patch` — with_tokens "vcurve"

完整论证（用户定调、根因、修复设计）见 git 历史中 v3 版本（commit 53fbad83）。
设计与纪律：配置门控 vital_curve 默认 false 逐位不变；守门 40 不触碰。
