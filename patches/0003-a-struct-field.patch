From: xf8410/umaai-rs-fanfix overflow-slack investigation
Subject: [PATCH 0003-a v2] struct 字段 vital_curve + vital_curve_years（参数化价格表，单 hunk）

v2：硬编码阶梯改为 [f32;4] 配置数组（乙线：2.5 版实测 −0.34%，需无重补丁调参）。
手验行数：old=2（1 删 + 1 ctx），new=8（7 增 + 1 ctx）。

--- a/crates/umasim/src/trainer/local_ramen_trainer.rs
+++ b/crates/umasim/src/trainer/local_ramen_trainer.rs
@@ -330,2 +330,8 @@
-    pub dynamic_special_targets: bool
+    pub dynamic_special_targets: bool,
+
+    /// 分年体力曲线门控（fanfix 0003）：开时 vital_factor 按年取 vital_curve_years，
+    /// 并对训练回体力对称计值（现行把 +vital 吞成 0）。`false`（默认）= 旧行为逐位保留。
+    pub vital_curve: bool,
+    /// 分年体力单价 [Y1, Y2, Y3, URA]（仅 vital_curve=true 时读取）。
+    pub vital_curve_years: [f32; 4]
 }
